import { useEffect, useRef } from 'react';
export function CustomCursor(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const node=ref.current;if(!node)return;const move=(e:MouseEvent)=>{node.style.transform=`translate(${e.clientX-24}px,${e.clientY-24}px)`};addEventListener('mousemove',move);return()=>removeEventListener('mousemove',move)},[]);return <div ref={ref} className="custom-cursor pointer-events-none fixed left-0 top-0 z-[90] hidden size-12 rounded-full bg-[conic-gradient(from_90deg,#FF4DB8,#A855F7,#22D3EE,#FF4DB8)] opacity-60 blur-md mix-blend-screen lg:block"/>}
