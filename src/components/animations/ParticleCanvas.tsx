import { useEffect, useRef } from 'react';
export function ParticleCanvas(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const canvas=ref.current;if(!canvas)return;const ctx=canvas.getContext('2d')!;const colors=['#FF4DB8','#A855F7','#22D3EE'];let mouse={x:0,y:0};let particles=Array.from({length:90},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.7,vy:(Math.random()-.5)*.7,r:Math.random()*2+1,c:colors[Math.floor(Math.random()*3)]}));const resize=()=>{canvas.width=innerWidth;canvas.height=innerHeight};const move=(e:MouseEvent)=>{mouse={x:e.clientX,y:e.clientY}};let raf=0;const draw=()=>{ctx.clearRect(0,0,canvas.width,canvas.height);particles.forEach((p)=>{const dx=p.x-mouse.x,dy=p.y-mouse.y,d=Math.hypot(dx,dy);if(d<130){p.vx+=dx/d*.04;p.vy+=dy/d*.04}p.x=(p.x+p.vx+canvas.width)%canvas.width;p.y=(p.y+p.vy+canvas.height)%canvas.height;p.vx*=.99;p.vy*=.99;ctx.beginPath();ctx.fillStyle=p.c;ctx.shadowColor=p.c;ctx.shadowBlur=14;ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()});raf=requestAnimationFrame(draw)};resize();draw();addEventListener('resize',resize);addEventListener('mousemove',move);return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);removeEventListener('mousemove',move)}},[]);return <canvas ref={ref} className="absolute inset-0 h-full w-full opacity-70"/>}
