import { Cube, Menu, ShoppingCart, X } from 'lucide-react';
import { useEffect, useState } from 'react';
import { Link, NavLink } from 'react-router-dom';
import { useCartStore } from '../../store/cartStore';
import { Button } from '../ui/button';
const links=[['/','Home'],['/catalogo','Catálogo'],['/sobre','Sobre'],['/contato','Contato']];
export function Navbar(){const [scrolled,setScrolled]=useState(false);const [open,setOpen]=useState(false);const count=useCartStore((s)=>s.items.length);useEffect(()=>{const on=()=>setScrolled(scrollY>20);on();addEventListener('scroll',on);return()=>removeEventListener('scroll',on)},[]);return <header className={`fixed inset-x-0 top-0 z-40 transition ${scrolled?'bg-[#0F0F1A]/78 shadow-2xl shadow-purple-950/30 backdrop-blur-xl':'bg-transparent'}`}><nav className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Link to="/" className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-gradient-to-br from-pink-400 to-purple-500 shadow-lg shadow-pink-500/30"><Cube /></span><span className="font-heading text-2xl font-bold tracking-wide">Sanemz Models</span></Link><div className="hidden items-center gap-8 md:flex">{links.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`font-semibold text-slate-200 hover:text-cyan-200 ${isActive?'text-pink-300':''}`}>{label}</NavLink>)}<Link to="/catalogo" className="relative"><ShoppingCart/><span className="absolute -right-3 -top-3 rounded-full bg-pink-500 px-1.5 text-xs">{count}</span></Link><Link to="/personalizacao"><Button>Orçamento</Button></Link></div><button className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></nav>{open&&<div className="mx-4 mb-4 grid gap-3 rounded-3xl bg-[#161626]/95 p-5 md:hidden">{links.map(([to,label])=><Link onClick={()=>setOpen(false)} key={to} to={to}>{label}</Link>)}<Link to="/personalizacao"><Button className="w-full">Orçamento</Button></Link></div>}</header>}
