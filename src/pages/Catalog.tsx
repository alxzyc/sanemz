import { SlidersHorizontal } from 'lucide-react';
import { useMemo, useState } from 'react';
import { ProductCard } from '../components/catalog/ProductCard';
import { products } from '../data/products';
const categories=['Todos','Action Figures','Bustos','Miniaturas','Dioramas'];
export default function Catalog(){const [cat,setCat]=useState('Todos');const filtered=useMemo(()=>cat==='Todos'?products:products.filter((p)=>p.category===cat),[cat]);return <section className="mx-auto max-w-7xl px-4 pb-20 pt-32"><h1 className="gradient-text text-7xl">Catálogo</h1><div className="mt-10 grid gap-8 lg:grid-cols-[280px_1fr]"><aside className="neon-card sticky top-28 h-max rounded-3xl p-5"><h2 className="mb-5 flex items-center gap-2 text-2xl"><SlidersHorizontal/> Filtros</h2><label className="text-sm text-slate-400">Categoria</label><div className="mt-2 grid gap-2">{categories.map((c)=><button key={c} onClick={()=>setCat(c)} className={`rounded-2xl px-4 py-3 text-left ${cat===c?'bg-pink-500/25 text-pink-200':'bg-white/5'}`}>{c}</button>)}</div>{['Material','Tamanho','Preço','Cor'].map((f)=><div key={f} className="mt-6"><label className="text-sm text-slate-400">{f}</label><div className="mt-2 rounded-2xl border border-white/10 bg-white/5 p-3 text-slate-300">Todos</div></div>)}</aside><div className="grid grid-cols-2 gap-4 md:grid-cols-3 xl:grid-cols-4">{filtered.map((p)=><ProductCard key={p.id} product={p}/>)}</div></div></section>}
